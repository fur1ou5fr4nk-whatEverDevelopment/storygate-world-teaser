{
  "name": "storygate-world-teaser",
  "private": true,
  "scripts": {
    "prepare": "node scripts/install-hooks.mjs",
    "test": "node --test tests/*.test.mjs",
    "release:check": "node scripts/release-check.mjs"
  },
  "devDependencies": {
    "parse5": "^8.0.1",
    "yaml": "^2.9.0"
  }
}
